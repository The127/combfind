; NOTE: used as documentation only — index.py walks the AST directly.

; import com.example.Foo
; import com.example.Foo.Companion
; import com.example.foo.bar
(import
  (qualified_identifier) @import.module
) @import.def
